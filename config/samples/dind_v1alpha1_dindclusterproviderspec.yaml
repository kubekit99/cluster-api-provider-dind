apiVersion: dind.kubekit.cloud/v1alpha1
kind: DindClusterProviderSpec
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: dindclusterproviderspec-sample
spec:
  # Add fields here
  foo: bar
